name: Build Fiora Docker image

on:
  push:
    branches:
      - music
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: fiora-image-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish image
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ghcr.io/ichinose520122/fiora:music
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and publish integrated music service
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile.netease
          platforms: linux/amd64
          push: true
          tags: ghcr.io/ichinose520122/fiora:music-api
          cache-from: type=gha,scope=music-api
          cache-to: type=gha,mode=max,scope=music-api
